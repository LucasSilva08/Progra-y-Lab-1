/** \brief funcion para pasar toda un array tipo char a mayuscula
 *
 * \param char[]recibe una cadena
 * \return void devuelve la cadena con los datos en mayuscula
 *
 */
void pasarMayuscula(char*);
/** \brief funcion para pasar un array tipo char a minuscula
 *
 * \param char[] recibe una cadena
 * \return void devuelve la cadena con los datos en minuscula
 *
 */
void pasarMinuscula(char*);
